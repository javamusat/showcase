{
  "name": "showcase",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "react-app:build": "yarn workspace @showcase/react-app build",
    "react-app:eject": "yarn workspace @showcase/react-app eject",
    "react-app:ipfs": "yarn workspace @showcase/react-app ipfs",
    "react-app:start": "yarn workspace @showcase/react-app start",
    "react-app:test": "yarn workspace @showcase/react-app test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@types/styled-components": "^5.1.7"
  }
}
